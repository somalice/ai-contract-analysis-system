# v1.0.0 Final Release Checklist

> **版本**: v1.0.0（Enterprise AI 企业级增强 · Sprint 8）
> **检查日期**: 2026-08-12
> **检查方式**: Git 工作区核查 / 后端 API 冒烟回归 / 前端构建 + 浏览器页面级回归 / SQLite 数据一致性只读脚本 / 配置文件人工核查 / 临时文件清单扫描
> **执行原则**: 本阶段只检查、不修改正式业务代码；发现的问题按 FAIL / WARN 分类列出，阻塞项另行处理

---

## 结论速览

| 检查域 | 结果 |
|---|---|
| 功能回归（后端） | ✅ PASS 27/27 |
| 前端回归（构建 + 页面） | ✅ PASS（build 0 error，14/14 页面） |
| 数据库一致性 | ✅ PASS |
| RBAC 权限 | ✅ PASS |
| RAG / AI 评估状态 | ⚠️ WARN（评估状态 FAIL，历史已知，非框架缺陷） |
| 配置检查 | ⚠️ WARN（见 §6） |
| 敏感信息检查 | ✅ PASS（API Key 未入库） |
| 版本号检查 | ⚠️ WARN（package.json / run.py 打印未统一） |
| 临时文件清单 | ✅ 已列清单（未删除） |
| 正式封版 | ⚠️ 允许封版，但需先完成 §10 收尾动作 |

---

## 1. 功能回归（后端）

环境：Flask 运行于 `http://127.0.0.1:5001`（application factory + Blueprint，配置自 `backend/.env`）。

### 1.1 启动与健康检查

| 项目 | 结果 | 说明 |
|---|---|---|
| Flask 启动 | ✅ PASS | 应用正常监听 0.0.0.0:5001 |
| `GET /api/v1/health` | ✅ PASS | `{"code":200,"data":{"status":"ok"}}` |

### 1.2 模块 API 冒烟（只读回归，admin token，共 27/27 PASS）

| 模块 | 接口 | 结果 |
|---|---|---|
| 认证 | `POST /auth/login` / `GET /auth/profile` | ✅ PASS |
| 合同管理 | `GET /contracts`、`GET /contracts/1/fields`、`GET /contracts/1/reviews` | ✅ PASS |
| 合同审核 | `GET /reviews` | ✅ PASS |
| 合同生成 | `GET /generation/history` | ✅ PASS |
| 模板中心 | `GET /templates` | ✅ PASS |
| 招标文件 | `GET /bids`、`GET /bids/1/requirement` | ✅ PASS |
| 投标文件 | `GET /proposals` | ✅ PASS |
| 知识库 | `GET /knowledge` | ✅ PASS |
| RAG 问答 | `POST /rag/query`（真实检索） | ✅ PASS |
| AI 评估 | `GET /evaluation/summary`、`GET /evaluation/history` | ✅ PASS |
| Prompt 管理 | `GET /prompts` | ✅ PASS |
| 操作日志 | `GET /logs/operations` | ✅ PASS |
| AI 调用日志 | `GET /logs/ai` | ✅ PASS |

> 说明：`POST /rag/query` 为真实检索调用（RAG_ANSWER_MODE=extract 纯抽取、零 LLM 成本），验证通过。

---

## 2. 前端回归

### 2.1 构建

`npm run build`（vite v5.4.21）→ ✅ PASS，`✓ built in 10.87s`，0 error。

### 2.2 页面级回归（浏览器实测，14/14 PASS）

| # | 页面 | 路由 | 标题规范 | 刷新 | Console error |
|---|---|---|---|---|---|
| 1 | 首页 | /dashboard | 仪表盘 - 智能合同与投标管理平台 | ✅ | 0 |
| 2 | 合同列表 | /contracts | 合同列表 - … | ✅ | 0 |
| 3 | 合同审核 | /reviews | 合同审核 - … | ✅ | 0 |
| 4 | 合同生成 | /generation/create | 合同生成 - … | ✅ | 0 |
| 5 | 招标文件 | /bids | 招标文件 - … | ✅ | 0 |
| 6 | 投标文件 | /proposals/history | 投标文件 - … | ✅ | 0 |
| 7 | 投标生成 | /proposals/create | 投标生成 - … | ✅ | 0 |
| 8 | 知识库 | /knowledge | 知识库 - … | ✅ | 0 |
| 9 | RAG 问答 | /knowledge/playground | RAG 问答 - … | ✅ | 0 |
| 10 | Prompt 管理 | /prompts | Prompt 管理 - … | ✅ | 0 |
| 11 | 模板中心 | /templates | 模板中心 - … | ✅ | 0 |
| 12 | AI 评估 | /evaluation | AI 评估 - … | ✅ | 0 |
| 13 | AI 调用日志 | /logs/ai | AI 调用日志 - … | ✅ | 0 |
| 14 | 操作日志 | /logs/operations | 操作日志 - … | ✅ | 0 |

- 一级菜单（5 项）：首页 / 合同管理 / 招投标管理 / 知识管理 / 系统管理 — 与最终信息架构一致 ✅
- 二级菜单（13 项）：与任务书清单完全一致 ✅
- Breadcrumb：全站无全局面包屑组件（确认按约定记录，非缺陷）
- 页面刷新：全部停留在原路由、正常渲染、无 404 ✅
- JS console：无运行时异常 / 无 CORS 报错 / 无 API 500（仅有导航切换产生的 `net::ERR_ABORTED` 正常提示）✅

---

## 3. 数据库检查（SQLite：backend/instance/app.db）

只读脚本 `backend/_release_db_check.py` 检查结果（2026-08-12 执行）：

| 检查项 | 要求 | 实际 | 结果 |
|---|---|---|---|
| 孤儿 bid_requirement（无对应 bid_document） | 0 | 0 | ✅ PASS |
| 重复 bid_document_id | 0 | 0 | ✅ PASS |
| contract_fields 核心测试数据 | 正常分布 | 48 行（8 大字段） | ✅ PASS |
| prompt_templates 脏 active 数据 | 0 | 0（6 行，active=0） | ✅ PASS |
| evaluation history | 正常 | evaluation_reports=44、evaluation_tasks=16 | ✅ PASS |
| operation_logs | 正常 | 128 行（user_login=100 主导） | ✅ PASS |
| ai_request_logs | 正常 | 948 行（rag success=939） | ✅ PASS |
| 核心表 | 齐备 | users=5 / contracts=8 / knowledge_documents=341 / knowledge_chunks=1610 / bid_documents=2 / generated_proposals=1 | ✅ PASS |

---

## 4. RBAC 检查

| 检查项 | 结果 |
|---|---|
| admin 登录（admin/123456） | ✅ PASS |
| employee 登录（employee/123456） | ✅ PASS |
| employee 访问 /prompts → 403 | ✅ PASS |
| employee 访问 /logs/operations → 403 | ✅ PASS |
| employee 访问 /logs/ai → 403 | ✅ PASS |
| employee 访问 /evaluation/history → 403 | ✅ PASS |
| employee 访问 /templates → 200 | ✅ PASS |
| employee 访问 /contracts、/knowledge、/bids → 200 | ✅ PASS |
| 前端菜单过滤（employee 隐藏 Prompt 管理/AI评估/AI调用日志/操作日志） | ✅ PASS |
| 前端路由守卫（直接访问受限 URL → 重定向 /dashboard + 提示） | ✅ PASS |

结论：前端菜单过滤 + 前端路由守卫 + 后端 role_required 三重 RBAC 链路全部生效。

---

## 5. RAG / AI 评估状态

| 检查项 | 状态 | 说明 |
|---|---|---|
| RAG 问答链路 | ✅ PASS | 真实检索返回 200，RAG_ANSWER_MODE=extract 零 LLM 成本 |
| RAG 缓存命中链路 | ✅ PASS | CacheService 正常（Redis 不可用时内存降级） |
| AI 评估功能 | ✅ PASS | /evaluation/summary、/history 正常；报告 44 份、任务 16 个 |
| AI 评估指标状态 | ⚠️ WARN | 最近一次评估结论 **FAIL（未达标）**；归因：知识库内容不足导致 RAG 命中率低 + 历史 JSON 解析遗留问题，**非框架/代码缺陷**（Sprint 8.5 已论证）。属业务数据状态，封版前如需达标需补充知识库语料并重跑评估 |

---

## 6. 配置检查

| 检查项 | 要求 | 实际 | 结果 |
|---|---|---|---|
| .env 是否存在敏感信息 | 允许本地存在，禁止入库 | 含真实 DeepSeek API Key + JWT_SECRET_KEY | ⚠️ WARN（已确认被 .gitignore 忽略，不入库） |
| .env.example 完整性 | 覆盖全部生产配置 | **不完整**：缺 JWT_SECRET_KEY / RAG_ANSWER_MODE / RAG_EXTRACT_TOP_N / RAG_CONTEXT_COMPRESS / LLM_CONNECT_TIMEOUT 等新配置；头部注释仍为 v0.6.0 | ⚠️ WARN |
| API Key 不得提交 Git | 已忽略 | 根 .gitignore 含 `.env`、`.env.local`，`git status` 无 .env | ✅ PASS |
| debug 是否关闭 | 生产关闭 | `backend/run.py` **硬编码 `app.run(debug=True, ...)`**；`.env` 中 FLASK_ENV=development | ⚠️ WARN |
| 正式配置与代码一致 | 一致 | settings.py `get_config()` 按 FLASK_ENV 返回对应 Config；RAG_ANSWER_MODE 读取正确 | ✅ PASS |
| RAG_ANSWER_MODE 生产配置 | 记录 | **`RAG_ANSWER_MODE=extract`**（句级抽取、零 LLM 成本，Sprint 8.9 结论最优）；RETRIEVER_TOP_K=10 / RERANK_FINAL_TOP_K=3 / EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5 | ✅ PASS（已记录） |
| JWT_SECRET_KEY | 生产更换 | `dev-jwt-secret-change-me-in-production`（开发占位值） | ⚠️ WARN |
| 前端生产环境变量 | 无敏感信息 | frontend/.env.production：`VITE_API_BASE_URL=/api/v1`（同域 Nginx 反代） | ✅ PASS |

---

## 7. 敏感信息检查

| 检查项 | 结果 |
|---|---|
| `.env`（含 API Key）是否被 git 跟踪 | ✅ PASS —— 未跟踪，`.gitignore` 已忽略 `.env`/`.env.local` |
| 代码中是否硬编码 API Key | ✅ PASS —— 统一从 `backend/.env` + settings.py 读取 |
| `git status` 中是否出现敏感文件 | ✅ PASS —— 无 .env、无凭据文件 |
| 日志是否输出敏感信息 | ✅ PASS —— 登录失败仅记录 username，不记录密码 |

> 注意：storage/models 下的 embedding 模型文件（bge-m3 / bge-large-zh-v1.5 / bge-reranker-base / bge-small-zh-v1.5，GB 级）**未被 .gitignore 忽略**，且 `backend/storage` 与根 `storage` 疑似存在两份拷贝 —— 见 §10 收尾动作（必须加忽略规则，否则 git add -A 将尝试提交 GB 级二进制）。

---

## 8. 临时文件清单（仅列清单，未删除）

### 8.1 backend/ 根目录（一次性/调试脚本，全部位于 app/ 正式包之外）

| 文件 | 类型 |
|---|---|
| `_release_db_check.py`、`_release_api_smoke.py` | 本次 Freeze 新建检查脚本（建议保留至封版动作完成或归档 scripts/） |
| `check_bid1_status.py`、`check_database.py` | 调试脚本 |
| `cleanup_contract_review_prompt.py`、`diagnose_prompt_cleanup.py` | Prompt 清理/诊断脚本 |
| `reset_bid1_to_draft.py`、`verify_review_contract7.py` | 数据修复/验证脚本 |
| `run_api_regression.py` | API 回归脚本 |
| `test_field_extraction.py`、`test_prompt_flow.py`、`test_sprint6_integration.py`、`test_sprint6_dbcheck.py` | 测试脚本 |

### 8.2 backend/tests/ 与 backend/migrations/

- `backend/tests/`：sprint5~8 各阶段测试文件（`*test*` 已被 .gitignore 忽略，不入库）
- `backend/migrations/`：sprint6/sprint7 一次性迁移脚本（已完成使命，可归档）

### 8.3 缓存 / 备份

- `backend/instance/app.db.bak_sprint6_version`、`app.db.bak_sprint7_1_20260807015325`（数据库备份，.gitignore 已忽略 *.bak）
- `backend/evaluation/cache/evaluation_context_cache.json`、`evaluation_embedding_cache.json`
- `backend/storage/vectorstore/knowledge.faiss` + `.meta.json`（向量库，正式运行依赖，**非临时**）
- 各目录 `__pycache__/`（已忽略）

### 8.4 scripts/ 实验产物（Sprint 8.9 RAG 调优实验，量大）

- 调优脚本：`s89_*.py`（analyze/diag/verify/summary 等 15+）、`run_rag_experiment.py`、`run_phase4_eval.py`、`debug_rag_sample.py`、`p4_*.py`
- 实验输出：`s89_*_prod.json/.md`、`p4_*.json/.md`、`phase*.out`、`s89_*_config.json`（约 60+ 文件）
- 评估脚本：`run_ai_evaluation.py`（正式交付，Sprint 8.5）、`init_enterprise_knowledge.py` / `init_evaluation_knowledge.py`（知识初始化，正式交付）、`eval_embedding_calibration.py`（实验）、`test_evaluation_api.py` / `test_evaluation_run.py`（测试）
- 缓存：`scripts/evaluation_embedding_cache/`、`scripts/_diag_cp_out.txt`

### 8.5 模型文件（大体积，见 §7 警告）

- 根 `storage/models/` 与 `backend/storage/models/` 两份：bge-large-zh-v1.5 / bge-m3 / bge-reranker-base / bge-small-zh-v1.5（本地 embedding/rerank 模型，运行依赖，但未被 .gitignore 忽略）

> **处置建议（本阶段不执行）**：临时脚本/实验产物可归档至 `scripts/_archive/` 或删除；模型文件必须在提交前加入 .gitignore。

---

## 9. 版本号检查

### 9.1 已统一到 v1.0.0（用户可见）

| 位置 | 状态 |
|---|---|
| Dashboard（constants.js `APP_VERSION`） | ✅ v1.0.0 |
| README.md 头部「当前版本」 | ✅ v1.0.0 |
| CHANGELOG.md [v1.0.0] 条目 | ✅ 存在 |
| docs/API_DESIGN.md「当前版本」 | ✅ v1.0.0 |
| index.html `<title>` | ✅ 无版本号（正常） |

### 9.2 存在旧版本残留（需统一）

| 位置 | 残留 | 级别 |
|---|---|---|
| frontend/package.json `version` | `"0.4.0"` | ⚠️ WARN（npm 包版本应 = 1.0.0） |
| backend/run.py 启动打印 | 「工程化版本 v0.2.0」 | ⚠️ WARN |
| backend/.env 头部注释 | v0.4.0 Sprint 2（内部注释） | ℹ️ 低 |
| backend/.env.example 头部注释 | v0.6.0 Sprint 4（内部注释） | ℹ️ 低 |
| README.md 正文 | v0.9.0「当前架构(Sprint 7)」等历史章节 | ℹ️ 低（历史演进记录，非当前版本声明；如追求严格可在正式版 README 中标注「历史版本」） |

> 结论：所有「当前版本」声明均已 v1.0.0；残留集中在 npm 包版本号、启动打印文案与注释，属收尾小改。

---

## 10. FAIL / WARN 汇总

### ❌ FAIL（阻塞项）

无阻塞性功能缺陷。

### ⚠️ WARN（封版收尾动作，建议正式发布前完成）

1. **Git 提交缺失（最高优先级）**：仓库仅 1 个 initial commit；正式工程 `backend/`、`docs/`、`frontend/`、`scripts/`、`CHANGELOG.md`、`README.md` 全部 untracked，`.env.example`/`.gitignore` 有未暂存修改。封版必须完成 `git add` + commit（或生成 release tag 基线）。
2. **模型文件入库风险**：`storage/models/`（含 backend 副本）GB 级二进制未被 .gitignore 忽略；执行 `git add -A` 前必须补充忽略规则。
3. **debug 未关闭**：`backend/run.py` 硬编码 `debug=True`；`FLASK_ENV=development`。正式部署需 production 配置（可接受：由启动命令/环境切换）。
4. **.env.example 不完整**：缺 JWT_SECRET_KEY、RAG_ANSWER_MODE、RAG_EXTRACT_TOP_N、RAG_CONTEXT_COMPRESS、LLM_CONNECT_TIMEOUT 等新配置说明。
5. **JWT_SECRET_KEY 为开发占位值**：生产部署必须更换随机密钥。
6. **版本文案残留**：package.json version 0.4.0、run.py 打印 v0.2.0（见 §9.2）。
7. **AI 评估状态 FAIL**：业务数据层面未达标（知识库语料不足导致 RAG 命中率低），非代码缺陷；如需达标需补充语料重跑评估。

### ℹ️ 备注（非问题）

- README/CHANGELOG/API_DESIGN 中历史版本章节属版本演进记录，保留合理。
- `*test*`/`*debug*`/`*diagnose*` 等 .gitignore 规则使 backend/tests/ 与调试脚本自动不入库——符合"测试文件不随正式工程提交"的预期，但如希望测试资产入库需调整规则。

---

## 11. 是否允许正式封版

### 结论：✅ 允许封版（代码质量层面），但须先完成以下收尾动作后方可打 tag

| 动作 | 是否阻塞封版 |
|---|---|
| 完成 Git 提交（backend/docs/frontend/CHANGELOG/README 入库） | 是（必须） |
| 补充 .gitignore 忽略 storage/models 后再 add | 是（必须，防 GB 二进制入库） |
| package.json version → 1.0.0；run.py 打印 → v1.0.0 | 是（版本一致性） |
| 生产部署切换 FLASK_ENV=production / debug 关闭；更换 JWT_SECRET_KEY | 是（仅部署时） |
| .env.example 补齐新配置项 | 建议 |
| AI 评估重跑达标（补充知识库语料） | 建议（不影响代码封版） |

### 判定依据

- 后端 15 个模块 + RBAC 共 **27/27 API 冒烟 PASS**；
- 前端 **build 0 error + 14/14 页面浏览器级 PASS**（菜单/路由/标题/刷新/权限/console）；
- 数据库一致性 8 项 **全部通过**（孤儿/重复/脏数据均为 0）；
- 敏感信息（API Key）确认未入库；
- 无阻塞性功能缺陷、无架构问题。

**本报告仅检查，未对正式业务代码做任何修改。** 上述收尾动作均为 Git/配置/版本文案层面，可按 §10 优先级在封版动作阶段处理。
